{
  "name": "suduko",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "serve": "npx http-server frontend -p 3000 -c-1 --cors",
    "build:web": "echo \"Static assets live in ./frontend\"",
    "cap:sync": "npx cap sync",
    "cap:add:android": "npx cap add android",
    "cap:copy": "npx cap copy android",
    "cap:open:android": "npx cap open android",
    "cap:assemble-debug": "cd android && ./gradlew assembleDebug"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/cli": "^8.0.0",
    "@capacitor/core": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5"
  }
}